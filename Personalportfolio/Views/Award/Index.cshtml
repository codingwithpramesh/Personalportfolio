@*@model Awards

@{
    ViewData["Title"] = "Edit award";
}


<form asp-action="Edit" method="post" enctype="multipart/form-data">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="row">
        <div class="col-md-8 offset-2">
            <div class="form-group">
                <label asp-for="Id">Id</label>
                <input asp-for="Id" class="form-control" readonly />
                <span asp-validation-for="Id"></span>
            </div>
            <div class="form-group">
                <label asp-for="Description">Description</label>
                <input asp-for="Description" class="form-control" />
                <span asp-validation-for="Description"></span>
            </div>




            <div class="form-group">
                <label asp-for="UId"> UId</label>
                <input type="datetime" asp-for="UId" class="form-control" />
                <span asp-validation-for="UId"></span>
            </div>
            <div class="form-group" style="margin-top:20px;">
                <button type="submit" asp-action="Edit" class="btn btn-primary">Create</button>
            </div>
        </div>
    </div>

</form>
*@


@model List<Awards>

@{
    ViewData["Title"] = "List Of Data";
}


<div class="container">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
</div>

<a asp-action="Create">Create</a>



<div class="row">
    <div class="col-md-8 offset-2">
        <table class="table">
            <tr>
                <td>@Html.DisplayNameFor(Model =>Model.First().Id) </td>
                <td>@Html.DisplayNameFor(Model =>Model.First().Description) </td>
                <td>@Html.DisplayNameFor(Model =>Model.First().UId) </td>

                <td>Actions</td>
            </tr>

            @foreach (var abc in Model)
            {
                <tr>
                    <td> @Html.DisplayFor(Model => abc.Id)</td>
                    <td> @Html.DisplayFor(Model => abc.Description)</td>
                    <td> @Html.DisplayFor(Model => abc.UId)</td>


                    <td> <a asp-action="Edit" asp-route-id="@abc.Id">Edit</a></td>
                    <td> <a asp-action="Delete" asp-route-id="@abc.Id">Delete</a></td>
                    <td><a asp-action="Details" asp-route-id="@abc.Id">Details</a></td>
                </tr>
            }


        </table>
    </div>
</div>




